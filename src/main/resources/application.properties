# Mysql
spring.datasource.url=jdbc:mysql://localhost:3306/moneymanager
spring.datasource.username=springstudent
spring.datasource.password=springstudent

server.servlet.context-path=/api/v1.0

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? Brevo HTTP API (ONLY THIS)
brevo.api.key=${BREVO_API_KEY}
brevo.sender.email=${BREVO_FROM_EMAIL}
brevo.sender.name=Money Manager

# JWT
jwt.secret=2b8e7c7e-4e2a-4b1a-9c2e-8f7e2d3c4b5a

# URLs
money.manager.frontend.url=${FRONT_URL}
app.activation.url=${BACKEND_URL}

spring.profiles.active=prod
